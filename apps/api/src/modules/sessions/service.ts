/**
 * Shopping session service (Phase 3).
 * Session create/end HTTP is deferred. Intent create validates sessions via
 * IntentsService + SessionsRepository (ownership + ACTIVE checks).
 */
export class SessionsService {
  // Intentionally empty until POST /sessions is implemented
}
