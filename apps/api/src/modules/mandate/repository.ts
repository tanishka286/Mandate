/**
 * Mandate repository (Phase 0 scaffold).
 * Will communicate with Supabase via the centralized client — no business logic yet.
 */
export class MandateRepository {
  // Intentionally empty in Phase 0
}
