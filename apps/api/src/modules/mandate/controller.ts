/**
 * Mandate controller.
 * Phase 2 Step 2: repository/service only — no HTTP mandate mutation surface.
 * Controllers must not access the database directly.
 */
export class MandateController {
  // Intentionally empty in Phase 2 Step 2
}
