/**
 * Mandate service (Phase 0 scaffold).
 * Controllers call services; services call repositories. No DB logic in controllers.
 */
export class MandateService {
  // Intentionally empty in Phase 0
}
